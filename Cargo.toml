[package]
name = "dlt-grep"
version = "0.1.0"
edition = "2024"
description = "A grep-like tool for DLT (Diagnostic Log and Trace) files"

[[bin]]
name = "dg"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
dlt-core = "0.20"
regex = "1"
